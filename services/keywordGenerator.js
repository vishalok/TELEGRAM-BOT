async function keywordGenerator(data) {
    // Mock keyword generation logic
    return [
        `${data.industry} trends`,
        `${data.objective} strategies`,
        `${data.location} marketing`,
    ];
}

module.exports = keywordGenerator;
